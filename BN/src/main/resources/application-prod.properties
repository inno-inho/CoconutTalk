spring.datasource.url=jdbc:mysql://db:3306/${DB_NAME:coconutTalk}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=${DB_ROOT_PASSWORD}

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 드라이버 설정
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver



###############################################
### 운영 배포 시 수정해야할 목록들
###############################################

# 1. ddl-auto 수정
# update는 테이블이 멋대로 바뀔 위험이 존재
# 운영 초기에는 update를 쓰기도 하지만, 안정화되면 validate
# spring.jpa.hibernate.ddl-auto=update 

# 2. SQL 로그 끄기 (운영 시 성능 향상)
# 개발 때는 true가 좋지만, 운영 시에는 너무 많은 로그가 쌓이므로 false를 권장
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.format_sql=false

# 3. 배포용 프로파일 적용 확인
# 환경변수 SPRING_PROFILE=prod가 제대로 주입되는지 확인